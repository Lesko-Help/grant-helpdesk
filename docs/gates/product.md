# Product

## What this repo is for
State it in the user's terms, one line, as it stands today — not what it
could become.

## Who uses it
One line: who runs it, or depends on its output, right now.

## The number that says it works
One line: a number you can check today that shows this repo is doing its
job (a query, a log count, a dashboard reading) — the current reading, not
a target.
