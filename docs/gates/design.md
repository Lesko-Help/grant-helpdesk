# Design

## File tree
The repo's file tree as it stands, one line per file or folder that
matters — not an exhaustive listing.

## Main functions and signatures
One line per function that matters: name, inputs, output type, as it is
today.

## Call stacks
One line per top-to-bottom call path worth knowing, in pseudocode, as the
code runs today.

## How it is tested
One line per test file or check that runs today, and what it proves.
