# Architecture

## Services
One line per service, queue or store this repo runs or talks to, as
deployed today.

## Endpoints
One line per URL a program calls today (method + path) — or "none" if this
repo exposes none.

## Data models
One line per schema or type that moves through this repo today (SQL
picture: a `CREATE TABLE` sketch, not the real DDL).

## Sequences
One line per "which calls which, in order" as the code runs today, left to
right — not a redesign.
