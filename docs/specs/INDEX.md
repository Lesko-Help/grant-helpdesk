# REPO-NAME

Three lines on what this app is.

## Goal
See `docs/specs/goal.md`.

## Architecture
See `docs/specs/architecture.md`.

## Modules

| Module | What it does | Deploy | Spec |
|---|---|---|---|
| NAME | one line: what it does | `deploy-NAME.sh` | `docs/specs/modules/NAME.md` |

## Open questions

One line each: something unresolved that a future session should pick up.

<!-- spec:template -->
